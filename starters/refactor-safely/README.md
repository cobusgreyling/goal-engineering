# Refactor Safely Starter

Scaffold with:

```bash
npx @cobusgreyling/goal-init . --pattern refactor-safely --tool grok
```

```
/goal Refactor per GOAL.md. Behavior-preserving; full test suite green; goal-verifier PASS.
```

Verifier locks behavior via full test suite and public API diff check.