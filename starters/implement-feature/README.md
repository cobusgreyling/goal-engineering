# Implement Feature Starter

Scaffold with:

```bash
npx @cobusgreyling/goal-init . --pattern implement-feature --tool grok
```

```
/goal Implement feature per GOAL.md acceptance criteria. goal-verifier before completed: true.
```

Verifier checks acceptance criteria checklist and new behavior tests.