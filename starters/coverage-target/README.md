# Coverage Target Starter

Scaffold with:

```bash
npx @cobusgreyling/goal-init . --pattern coverage-target --tool grok
```

```
/goal Raise coverage to GOAL.md threshold. goal-verifier confirms coverage report.
```

Verifier parses coverage output and rejects hollow tests.